# torch wheels lag new Python releases — leave the floor open so 3.12/3.13 both
# resolve. Pin exactly only if a specific build starts misbehaving.
torch>=2.6
sentence-transformers>=3.3
fastapi>=0.115
uvicorn[standard]>=0.34
